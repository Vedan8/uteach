import styles from "@/app/css/reach.module.css"
import Reachdata from "@/components/updates/reachdata";

export default function Reach() {
  return (
    <section className={styles.reach}>
      <Reachdata />
    </section>
  );
}